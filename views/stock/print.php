<?php

	// No direct access
	defined('_DMEXEC') or die( 'Restricted access' );

	class DMPrintStock extends DMPrintClass {
	
		function getInput() {
		}
		
		function execPrint($view, $template) {
		
			return parent::execPrint($view, $template);
		
		}
		
	}